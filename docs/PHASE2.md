# 二阶段开发需求

> **这份文档描述愿望，不描述现状。** 这一点和 `BACKEND.md` / `API.md` 相反 ——
> 那两份的维护规则是「先改代码再改文档、只写现状」，所以路线图不能塞进去。
>
> - 现状（后端）→ [BACKEND.md](./BACKEND.md)
> - 现状（接口契约）→ [API.md](./API.md)
> - 现状（前端）→ [apps/web/README.md](../apps/web/README.md) §10「交接」
>
> 二阶段开发以本文为锚。**每完成一项，回来把它划掉并写清落在哪** ——
> 一份没人更新的路线图比没有更糟：它看起来还有效。

---

## 0. 起点：一阶段（最小跑通）交付了什么

页面层十项全部上线并在生产验证：

```text
A1 登录 · C1 CSRF 开关 · B1/B2 前端上线
A5 任务列表/新建 · A6 任务详情 · A7 证据页 · A8 客户首页分流
A2 品牌管理 · A3 提问词管理 · A4 用户管理
```

测试：前端 156 条（`lib/l3` 138 + `lib/api` 18，全是纯函数）· 后端 274 条。
真实数据跑通：task 27「安踏监测集」· run 27（回填 35 样本）· run 54（10 样本全成功）。

**一阶段唯一没做的**：前端组件 / 端到端测试。它进了下面的 P2-01。

---

## 1. 一个判断：第二个平台是这一阶段的分水岭

`app/providers/registry.py` 那层设计得好，加平台**确实只改一个文件**。
但前端这一轮所有口径都建立在「只有一个平台」这个隐含假设上，
第二个平台一接就会同时引爆四件事：

1. **「提及率 60%」跨平台是什么意思？**
   DeepSeek 80% + 豆包 20% 混算出 50%，对运营没有任何操作价值 ——
   他要知道的是「豆包上我不行」。`/v1/counts` 早就支持 `group_by=platform`，
   但前端一次都没用过。
2. **命中矩阵要变形。** 现在是 提问 × 品牌。加一维就是三维，而三维矩阵不可读 ——
   所以大概率结论是「平台是筛选器，不是维度」。这个决定影响 A6 整页版式。
3. **`apps/web/README.md` §5「平台环形图不做」的理由当场失效**
   （原因写的是「只有一个平台，画出来是假装有多样性」）。
4. **job 数翻倍**，`services/counts.py` 那个「拉 `full_text` 进 Python 再累加」
   的债会提前咬人。

还有一条容易漏的：**每个平台一份会过期的登录态。**
DeepSeek 用 `storage_state`。过期的表现是一批 job 全 `failed` ——
而这和「平台没接」「网络问题」的排查方向完全不同，现在没有任何东西能区分它们。

**所以顺序是：先定口径，再写 Provider。** 反过来做，Provider 写完前端也接不上。

### 1.1 还有一个更靠前的前提：采集出口的地理位置

**已确认：出口 IP 会影响 AI 的回答。** 而 VPS 在新加坡（机房 IP），
服务对象是大陆用户，接下来要接的三个平台（豆包 · 千问 · Kimi）**全是大陆服务**。

两个后果，都很硬：

1. **数据口径本身就不对。** 从新加坡采到的回答不代表大陆用户看到的回答。
2. **调试时会有两个变量同时在动。** 从机房 IP 开发大陆平台的 Provider，
   抓失败时分不清是「Provider 写错了」还是「IP 被风控了」——
   而这两个的排查方向完全相反。这个问题会贯穿三个平台。

**所以采集出口迁移排在接平台之前**，见 §3「零、采集出口与部署拓扑」。

**⚠️ 迁移会造成基线断点。** run 27 / 54 与迁移之后的 run **不可比**。
迁移后第一次 run 的 `run.note` 必须写明「采集出口已迁至大陆」，
且 `P2-31` 趋势图要能表达这个断点 —— 一条跨越迁移点的折线画成连续的就是骗人。

---

## 2. 执行顺序

### 第一梯队 —— 做完才好动大的

| ID | 任务 | 状态 |
|---|---|---|
| **P2-01** | 端到端冒烟测试 | ✅ 两层：`apps/api/tests/test_data_invariants.py`（真实数据不变量，9 条）+ `scripts/smoke/browser_smoke.py`（浏览器只读路径，9 步） |
| **P2-02** | 任务编辑入口 | ✅ `app/(dashboard)/tasks/[id]/TaskEditPanel.tsx` |
| **P2-03** | 运行中自动刷新 | ✅ 6 秒轮询，只在有 run 未跑完时开，页面不可见时暂停 |
| **P2-04** | 缺口清单导出 CSV | ✅ `lib/l3/csv.ts` + `lib/l3/gap-export.ts` + `components/runs/ExportGapsButton.tsx` |

**第一梯队做完了。** 下一步是第二梯队的 **P2-05（定多平台口径）**——
那是个决策，不是编码，定不下来 P2-06 写完前端也接不上。

#### P2-01 的收成：冒烟第一次跑就抓到一个已上线的 bug

`first_offset` 是 Python 的**码点**索引，而前端用 `String.slice` 按 **UTF-16
单元**切 —— 一个 🏃 在 Python 里算 1、在 JS 里算 2。含 emoji 的回答（DeepSeek
很常见）证据页高亮全部错位。

**线上没有画错** —— `buildHighlights` 那条自检拦住了：对不上就不画、改报告警。
但代价是那些回答一律没有高亮。修法见 `apps/web/src/lib/l3/text.ts`；
顺藤还摸到同类第二处（`isPreviewTruncated` 拿码点数比 UTF-16 长度）。

**`docs/API.md` §7.1 那条不变量的写法本身就是错的**（写成了
`full_text.slice(...)`），它正是误导的源头，已更正为分别给出 Python 与 JS 两种写法。

这条记在这里，是因为它回答了「为什么冒烟值得先做」——
它抓的不是「按钮点不动」，是**跨语言口径分叉**这种任何单层测试都看不见的东西。

#### P2-02 顺带补的后端一致性

做任务编辑时发现 **`start_run` 不检查 `task.is_active`** —— 「已停用」只是任务
列表里的一个徽章，而 `create_run` **是**按 `Prompt.is_active` 过滤提问词的。
同一个字段名一处当真、一处装饰。

之前不明显，因为界面上从没有停用任务的入口。编辑入口一做出来，
不补这条就等于在界面上写一句假话。已补：停用的任务发起运行返回 400
（不是 404 —— 任务确实存在、你也看得见，只是它被停用了）。

### 第二梯队 —— 采集出口迁移 + 多平台

| ID | 任务 | 状态 / 依赖 |
|---|---|---|
| **P2-05** | 多平台口径 | ✅ **已拍板**，结论见 §4.0 |
| **P2-32** | 采集出口对照实验 | ✅ **已完成**，结论见 §3「零」 |
| **P2-33** | crawler 迁至家里云（**临时**） | ✅ `scripts/crawl-node/`（部署脚本 + 约束文档）· `BACKEND.md` §10.1 |
| **P2-35** | `POST /v1/tasks/{id}/runs` 接受 `note` | ✅ 后端 + 前端确认框里的输入框 |
| **P2-16** | 自动退避重试 | ✅ 代码 + **开关已开**（2026-08-15，`GEO_AUTO_RETRY=true`）。根因更正见 §3「零」 |
| **P2-08** | 失败原因分类 | ✅ **后端已落**（`services/failure_kinds.py`），前端展示留给 P2-09 |
| **P2-07** | **登录态健康度** | ✅ `services/credential_health.py` + `GET /v1/health/credentials`。**看的是「从哪儿签发的」**，不是「多久没成功」 |
| **P2-36** | 记录采集节点 → **采集环境** | ✅ `services/crawl_env.py` + `crawl_environments` 表 + `GET /v1/runs/{id}` 的 `environments[]`。**长度 > 1 就是混了两个出口** |
| **P2-06a** | 接豆包 | ⏸ **暂缓**（2026-08-16）。provider 本身成立（run 294 有 6 条 1000+ 字的好答案），但账号配额持续衰减 **6 → 3 → 1/5**，持久 profile 也没止住。**这已经是产品决策不是技术问题** —— 见下「第一次真抓」与「现象 C」 |
| **P2-37** | **联网标注 + 引用提取**（原名「联网搜索 + 新基线」） | ⬜ **前提已被推翻，等重新拍板**（2026-08-17）。「必须开联网」在豆包/千问上做不到；行业与学术都要求「记录并分层」而非强制。**重定义后成本降一个数量级：基线不作废、不必整套重抓**。引用走 SSE 已实测可行。见 §4.0.1 |
| **P2-34** | crawler 改 HTTP worker（**正式**） | ⬜ **退役条件要重述**：原文写「接完豆包就做」，而豆包暂缓了。现在的触发条件应是「P2-37 重抓之前」—— 否则那两笔债（截图关闭、数据库凭证走家宽）会跟着无限期留着 |
| **P2-09** | 前端多平台改造 | ✅ **已完成并在双平台 run 上验证**（2026-08-17，run 297）。`lib/l3/platforms.ts` + `TaskDetailView` 三处渲染 + `fetchRunCounts` 的 `platform`。见下「P2-09 已完成」 |
| **P2-06b** | 接千问 | ✅ **已完成**（2026-08-16）。`providers/qianwen_web.py` + 注册表 + `TONGYI_*` 接线。run 296 **10/10 全成功、零衰减、6 分 20 秒**。见下「P2-06b 已完成」 |
| **P2-06c** | 接 Kimi | P2-06b |
| **P2-39** | **修两个会撒谎的告警** | ⬜ **新增（2026-08-17）**。P2-07 把短命 cookie（`passport_csrf_token`）当过期判据 → 千问正在成功抓取却标 `expired`；P2-36 的 `exit_ip` 探测失败会造出幻影环境 → 误触发「混了两个出口」。**告警撒谎比没有告警更糟** |
| **P2-38** | **提高样本量** | ⬜ **新增（2026-08-17）**。现在 `samples=1`、每平台 10 条，而公开建议是每 prompt 重复 7–8 次起步、稳定频率要 60–100 次。run 297 那个「首位提及率 14.3% vs 42.9%」分母只有 7 —— **差两条样本**。**它比 P2-37 更影响结论可信度**，见 §4.0.1 第六节 |

#### ⚠️ 豆包这条路可能不成立（2026-08-15 实测）

**接豆包不是「把 DeepSeek 的选择器换一套」，风险级别不一样。**

第一条探针提问发出后，页面直接跳 `?from_logout=1`，登录态当场失效。
在采集节点容器里实测，crawler 的浏览器有四个明着写着「我是自动化」的破绽
（`webdriver=True` · `plugins=0` · `window.chrome=undefined` · UA 里带
`HeadlessChrome`）。豆包同时加载了 `s2-security-audit` / `-verify` / `-message`
三个模块 —— 它在**主动做检测**，而 DeepSeek 一年没咬过我们。

已做的加固见 `BACKEND.md` §7.5（新 headless + 参数 + UA + persistent context），
节点实测四项全过。**但这只说明今天能过。**

| | DeepSeek | 豆包 |
|---|---|---|
| 反自动化 | ~~一年没遇到~~ → **2026-08-17 账号被禁言**（见下） | **未加固时发一条就登出** |
| 答案气泡 | `.ds-markdown` | `.md-box-root` + `[data-message-id]` |
| **完成判定** | 长度连续不变轮询 | **`[data-streaming]` / `data-complete` 状态位** |
| 输入框 | `textarea` | `textarea` 有两个，**第二个是 `aria-hidden` 的隐藏替身** |
| 首页 / 输入区 | 语义类名 | 生成式工具类，只能靠 `aria-label` + `placeholder` |

> **一处更正（同日）**：最初判断「豆包无 `data-testid`，只有生成式工具类」——
> 那是**首页与输入区**的情况。**对话区有完整的 `data-*` 体系**（48 个属性名），
> 答案气泡的 `md-box-root` 是稳定语义类。**这一点上豆包不比 DeepSeek 脆，
> 完成判定甚至更好。**留着那个夸大的判断会让下一个人高估工作量。

**两条仍然成立的判断：**

1. **反自动化是一场会持续的对抗。** 加固之后今天能过（实测：persistent +
   新 headless + UA + 参数，发提问不再登出），但不代表下个月能过。
   每次失效的表现都是「一批 job 全 failed」——
   P2-08 会归成 `parse_error`（不重试，分类是对的），但看不出根因。
2. **如果某次改版之后长期过不去，那时要重新讨论的是「豆包还做不做」，
   不是「再加一层伪装」** —— 本仓不做 stealth 注入，
   理由见 `providers/browser.py` 的模块 docstring。

**为什么把 P2-07 也提到豆包之前**（2026-08-15 改的顺序，原计划它依赖 P2-06a）：

一份**过期 11 天**的登录态悄悄让每次 run 丢掉头几条，而没有任何地方报警 ——
这正是 P2-07 要防的东西，它刚刚真实发生了一次，代价是三天的数据口径不对
（§3「零」）。而**接豆包会再多一份会悄悄过期的凭证**：先接平台再做健康度，
等于把同一个坑再挖一个。

现在还多了一个具体抓手：**WAF cookie 的类型能直接暴露「这份登录态是从哪儿建的」**
（`aws-waf-token` = 境外，`HWWAFSES*` = 境内）。这比原计划的
「上次成功抓取距今多久」信息量大得多 —— 后者只能发现「全红了」，
而这次的故障形态是**没全红，只是悄悄降级**。

**为什么把 P2-35 与 P2-16 提到豆包之前**：它们都会**污染豆包的调试**。
记不上口径 + 每次 run 莫名丢头几条，会让「豆包 Provider 到底写对没有」
这件事更难判断 —— 而调试时不能有多个变量同时在动，这也是迁移排在
接平台之前的同一条理由。

**P2-33 是临时方案，退役条件写死：接完豆包（P2-06a）就做 P2-34。**
不设条件它会永远留着 —— 而它带着两个不该长期存在的代价（数据库凭证跑在
家宽链路上、截图降级）。

#### 部署已完成（2026-08-16），但**还没标 ✅**

采集节点镜像已更新到 `2026-08-16 00:21:46`（与 VPS 侧一致），
`deploy.sh verify` 五项全过，其中「容器出口 `120.228.64.174` ≠ 代理出口
`42.200.172.28`」—— 那条唯一会静默毁掉整件事的检查过了。
`GET /v1/health/credentials` 现在是两行：

```
deepseek  ok  cn       huawei
doubao    ok  unknown  none      ← unknown 是预期，不是故障
```

`unknown` 是因为豆包用字节自家的 `ttwid`/`sessionid` 体系，28 个 cookie
里没有任何 WAF cookie，所以认不出签发地。`credential_health.derive_status`
对 `unknown` **刻意不报 mismatch**（`BACKEND.md` §7.3 第 3 条）——
这条设计正是为接第二个平台写的，现在它第一次真的被用上了。

⚠️ **实测任务必须只勾豆包一个平台。** 前端 `fetchRunCounts` 不带 `platform`
参数，勾两个平台会让 KPI 与命中矩阵**静默混算** —— 平台筛选器要等 P2-09。
（**后端不缺这个能力**：`/v1/counts` 早就有 `platform` 参数，
`api/counts.py:25`，`group_by` 也已支持 `platform`。P2-09 是纯前端工作。）

#### 第一次真抓：run 294（2026-08-16）—— provider 成立，但挖出两个 bug

**结论先写：豆包 provider 是能用的。** 6 条 `ok`，1000–1700 字，
内容质量高（安踏 / 李宁 / 特步 / 361° / 乔丹，带价位与适用人群）。
**选择器、指纹加固、SSE 拦截都被证明是对的** —— 这一点不用再怀疑。

预定要盯的三处，结果：

| 看什么 | 结果 |
|---|---|
| `failure_kind` 是不是 `login_required` | **不是，一条失败都没有** —— 而这本身就是 bug A |
| `raw_json.session_deleted` | **全是 `false`** —— 见 bug B |
| `environments[]` | ✅ 19/20 打上同一环境 `changsha-home\|120.228.64.174\|Asia/Shanghai\|real\|cn\|huawei`，单一出口。未打标的 1 条是从没被领取的 pending。**P2-36 工作正常** |

**bug A（已修，commit `b6c4b56`）· 超时被伪装成成功。**
`_wait_for_answer` 超时后 `return prev`，而豆包不答时 `.md-box-root`
匹配到的是**用户自己那条提问气泡** —— 于是 11/17 条以「成功，答案 = 提问原文」
入库（`full_text` 11–16 字符，latency 全是 126 秒）。
靠 L1 的 `too_short` 才被发现，**但那是运气**：提问词长过阈值就会当成真答案。
已改成抛 `DoubaoAnswerTimeout(TimeoutError)` → `failure_kinds` 按类型归 `timeout`
→ P2-16 退避重排。

**bug B（未修）· 会话删除从来没成功过。**
`page.url` 一直停在 `https://www.doubao.com/chat/local_XXXX`，`local_` 是客户端
占位符，所以 `_delete_current_session` 每次都走「拿不到会话 id」分支返回 False。
**账号里每抓一条就堆一个对话，一条没删。** 这可能正是现象 C 的原因之一 ——
所以先别急着单独修它，等 C 定性。

**现象 C · 豆包在第 3 个提问词、第 6 条之后就不答了。**
前 6 条 35–47 秒正常，之后 10 条全部打满 120 秒。
**这就是为什么 A 必须先修** —— 失败全被伪装成成功时，重试机制根本不触发，
也就拿不到「退避之后还会不会失败」这个唯一能区分限流与封禁的信号。

**A 修完当天就拿到了第一组数据（job 532）**：修复上线后那条残留 job
连试 3 次（03:20 / 03:21 / 03:23，中间还隔了停机的约 20 分钟），
**三次全部超时**，每次都是同一句：

```
等答案超过 120000ms；页面上最长气泡 12 字符（还在 streaming=True）
```

三条推论：

1. **不是短时限流。** 跨越约半小时、四次尝试，一次都没恢复。
   限流该在这个尺度上放行。
2. **豆包收下了消息但永不给答案** —— `streaming=True` 一直在。
   不是拒答、不是报错，是**假装在生成**。这是典型的静默封锁形态。
3. **「最长气泡 12 字符」= 提问的长度**，直接印证了下面那条被证伪的判断。

**第二组数据（run 295，同日下午）把三个解释一次排除了。**
换了新 `storage_state` + 手工清空全部历史会话 + 隔了时间，**照样复现**，
而且配额从 6 条掉到 3 条：

| 曾经的解释 | 判定 |
|---|---|
| 短时限流 | ✗ 跨小时、跨会话都复现 |
| `storage_state` 降级 | ✗ 换新的之后撑得更短（6 → 3） |
| bug B 的会话堆积 | ✗ 清空之后照样复现 |

**「降速」这条预案也被证伪了。** `BACKEND.md` §7.2 第 4 条写着「大面积限流时
要的是全局降速/熔断」—— run 295 每条间隔 5–7 分钟，比 run 294 成功那 6 条的
**0.9 分钟慢了六七倍，结果反而更差**。所以这不是"单位时间请求太密"，
那条预案对这个对手不成立。惩罚是**按账号跨会话累积**的。

#### 外部情报（2026-08-16，经 Grok 实时检索）

社区材料**独立印证了两条我们自己推出来的判断**：

1. **持久化 profile 是已验证的做法。** `doubao2api` 有 `DOUBAO_BROWSER_DATA`
   配置，目的写明是「复用 `webid` / `device_id` / `msToken`，避免每次冷启动
   换指纹」—— 和我们从「老会话 + 空白设备自相矛盾」推出的结论一致。
2. **stealth 注入对豆包无效。** `lza6/doubao-2api` 标配 Playwright stealth，
   仍然中人机（#5）、签名超时（#7）；Linux.do 2026-05 有人用 Playwright MCP，
   结论是「效果更不佳，而且慢」。**本仓「不做 stealth 注入」那条原则，
   在这个对手身上不只是洁癖，是它确实不管用。**

另外三条值得记：

- 豆包**公开已知**的防线是 `a_bogus` 签名 + `msToken`(BDMS/Slardar) +
  `710022004` 验证码 + `webid`/`device_id`。**`s2-security-*` 那三个模块
  公开圈没人拆过** —— 我们看到的比社区讨论的更深一层。
- **容器无头本身可能就是一层问题**：`lza6/doubao-2api` #7（2026-05）报告
  「容器里校验函数不下发」。我们正好跑在容器里，这条还没验证。
- **社区没有任何已证明能破「累积配额」的做法。** 多号轮询是主流，
  但作者自己写「不保证不被关联」。而我们这个「先答 3–6 条再阴死」的形态，
  **没找到任何独立复现报告** —— 它是本仓的一手观测。

**当前假说与它的可证伪预测**：诱因是「每条 job 一个全新临时 profile」造成的
设备身份断裂（已改，commit `7f18725`）。假说成立的话，**撑住的条数不该再衰减**。
验它要连续两三轮小批量看趋势，**单条 job 说明不了**。

**一处被证伪的判断（还没改代码）**：`_answer_text` 的 docstring 写着
「`.md-box-root` 只包答案」—— run 294 证明它**连用户提问气泡一起匹配**，
否则不答时 `count()` 应该是 0、`prev` 应该是空字符串、`search()` 会
`raise RuntimeError("empty answer")` 归成 `parse_error`。
修它要换选择器，而本仓的规矩是**选择器必须在节点上实测、不照抄** ——
手上没有那次的 DOM，猜一个反而更危险。下次真抓时连 DOM 一起取。

⚠️ **run 294 不能当基线用**（11/17 是垃圾）。

⚠️ **豆包登录态的 `earliest_expiry` 是 `2026-08-16T14:53:11Z`**（签发后约一天）。
28 个 cookie 里既有 `sessionid` / `sid_guard` / `ttwid` 这类会话核心，
也有 `passport_csrf_token` / `biz_trace_id` 这类短命的，所以这个时间
**大概率是后者在叫**。但健康度端点报的就是它 —— 明天它翻成告警时，
先确认是哪一条到期，别直接当成登录态失效去重新导出。

#### P2-06b 第 0 步：千问实测（2026-08-16，在采集节点上）

**为什么在豆包没打通时就去探千问**：产品要的是「多平台」，不是「必须是豆包」。
豆包的敌意已经明确，而「千问难不难」这个信息**现在就能拿到**，
不该等豆包打通才知道。探针是只读的，不登录、不提问品牌相关内容。

| | 豆包 | 千问（`qianwen.com`） |
|---|---|---|
| 登录 | 必须，会过期、会被扣配额 | **不登录也能问并拿到回答** |
| 首次响应 | 23–47 秒 | **10 秒** |
| 联网 + 引用 | **没有手动开关**（P2-37 的死结，见 §4.0.1） | **默认开，回答自带「N 篇来源」** |
| 风控脚本 | `s2-security-audit` / `-verify` / `-message` 三个专用模块 | 一个 `sufei_data/3.9.14`（阿里全站通用设备 ID SDK） |
| 会话 URL | 卡在 `/chat/local_XXXX`（服务端从没分配 ID） | 真会话 id |
| 入口 | `tongyi.com` → 跳 `qianwen.com` | — |

**实测到的选择器（未来写 provider 时直接用，别重新猜）：**

- 输入框：`[contenteditable='true']`，Slate 编辑器（`data-slate-editor`），
  `data-placeholder="向千问提问"`。**页面上没有 `textarea`。**
- ⚠️ **`click()` 会超时** —— 首屏大图（`absolute inset-0 h-[266px]`）盖在
  输入框中心点上，另外还有一个 `z-[1001]` 的全屏浮层。
  **`element.focus()` + `keyboard.type` 可行**，实测通过。
  （和豆包那个「两个 textarea，第二个是替身」是同类坑：都表现为 `click` 超时。）
- 答案气泡：`.answer-common-card` / `.chat-answers-card-wrap` /
  `.markdown-pc-special-class`；提问气泡是 `.message-card-wrap.question`。
  **`.chat-answers-card-wrap` 会把「N 篇来源」一起带进来**，取正文用前两个。

**三条对冲，别把「千问很轻松」当结论：**

1. **千问不是没风控。** 外部情报：`Qwen2API` 专门给每个账号配独立出站代理，
   理由写的是「规避 `chat.qwen.ai` **基于 IP 的关联封禁**」；另有约 128 KiB
   的 WAF 体积限制和 429。
2. **没有人有「Playwright 天天点页面稳定几个月」的现场记录。**
   社区长期方案几乎全是 token → HTTP，不是无头点 UI。
3. **国内 `qianwen.com` 与海外 `chat.qwen.ai` 是两套产品**，社区工具主攻后者。
   我们的客户是大陆用户，**只能用前者**，所以那些经验不能直接搬。

**连续 15 条的实测结果（同日）**：前 5 个对话正常（稳定 13.3 秒），
**第 6 条起发送被静默禁用** —— 文字打得进输入框，Enter 不提交，URL 不变，
页面上没有任何验证码 / 限流 / 登录提示。

**但这不是反爬，是匿名产品配额。** 两个证据：

1. **换一个全新 profile，同一个 IP，立刻就能问**，答案完整、会话 id 正常。
   → 额度记在**当前匿名会话**上，**IP 完全没被记账**。
2. 点发送按钮也没有任何弹窗或提示 —— 是静默失效，不是拦截。

**和豆包对照，这是两种性质完全不同的限制：**

| | 豆包 | 千问 |
|---|---|---|
| 惩罚记在哪 | **账号，跨会话累积**（换新登录态后更差：6 → 3） | **只记当前匿名会话**，换 profile 立刻清零 |
| IP | 外部情报称「IP 会被记一笔」 | **没被记账**（同 IP 新 profile 秒通） |
| 升级路径 | 静默不答 → `ERR_CONNECTION_CLOSED` → 强制跳转登出 | 只是发送变惰性，无连接层动作 |

⚠️ **仍然不能下「千问没有反爬」的结论。** 已验的是「匿名 5 个对话的产品配额」
和「IP 不被记账」，**没验的是登录态下大批量抓取会怎样** —— 而那才是生产形态。
用户已拍板可以登录，所以匿名配额本身不是障碍；
选型依据是**风控姿态**（一个通用指纹 SDK vs 三个专用检测模块、IP 不记账、
无连接层升级），不是「匿名能用」。

**还没验**：匿名回答与登录回答一不一样（**保真度问题** ——
若不同，交付口径要写清用的是哪一种）。

#### P2-06b 已完成（2026-08-16）：run 296 一次跑通 10/10

落在 `providers/qianwen_web.py` · `registry._build_tongyi` ·
`failure_kinds`（`QianwenLoginRequired`）· `deploy.sh` 的 `TONGYI_*`。

**工程侧的结果是硬的，和豆包不在一个量级：**

| | 豆包 run 294 | 豆包 run 295 | **千问 run 296** |
|---|---|---|---|
| 成功 | 6/20（11 条是垃圾） | 2/10 | **10/10** |
| 全程 | ~33 分钟 | 43 分钟 | **6 分 20 秒** |
| 请求间隔 | 0.9–2.3 分 | 5–7 分 | **0.6–0.8 分** |
| 衰减 | 6 → 3 → 1 | 是 | **没有** |

**最有说服力的是间隔**：千问的请求密度比豆包失败时还快七倍，却一条没掉，
latency 平在 27.7–35.8 秒、长度 1216–1876 字，全程没有走弱迹象。
`environments[]` 单一，10 条全打上标记。

**品牌指标（`/v1/counts`，安踏）**：

| run | 平台 | n | 提及率 | 首位率 | head/mid/tail |
|---|---|---|---|---|---|
| 27 | deepseek | 35 | 60.0% | 28.6% | 15/5/1 |
| 293 | deepseek | 20 | 55.0% | 36.4% | 8/2/1 |
| 294 | doubao | 7 | 85.7% | 0.0% | 6/0/0 |
| **296** | **tongyi** | **10** | **70.0%** | **28.6%** | 3/4/0 |

**怎么读**：70% 高于 DeepSeek 的历史区间（33–60%），但 **n=10**；
首位率 28.6% 与 DeepSeek 持平，不算突出。豆包那 85.7% 别当真（n=7，且首位 0%）。
跨平台比单个数字正是 §4.0 拍板时警告的事。
一个**可能有信号但还不够**的观察：千问把安踏更多放在中段（head 3 / mid 4），
而 DeepSeek 明显偏 head（run 27 是 15/5/1）—— n 变大之后还这样才算数。

> ⚠️ **算提及率只能走 `/v1/counts`，不能直接查 `mentions` 表。**
> 2026-08-16 我按 `EXISTS(SELECT 1 FROM mentions ...)` 查，得到 10/10，
> 差点当成「100% 提及」报出去。真相是 **`mentions` 里每个（响应×品牌）都有行**，
> `position_rank IS NULL` 的在 counts 里算 `m_none` —— 正确答案是 7/10。
> 顺带虚惊一场：一度以为文档里「35 条 60.0%」的基线过时了，
> 实际 counts 查出来正是 `21/35 = 60.0%`、`m_first 6`、`head 15/mid 5/tail 1`，
> 与 `API.md`/`BACKEND.md` 一字不差。**绕过 counts 自己算，就是在造第二套口径。**

**P2-37 相关**：run 296 的 10 条回答**没有一条带「N 篇来源」**，说明这批提问词
没触发千问联网。所以它与 DeepSeek 的历史基线**在「不联网」这一点上是一致的**，
可比性比预期好。

**两笔已知欠账**（都写在 provider 的模块 docstring 里）：
删会话没实现（侧栏找不到 `conv_id`，没验过的删除逻辑不写）→ 会话会堆积；
`citations` 恒为 `[]` → 留给 P2-37。

#### P2-09 已完成（2026-08-17）：混算掩盖了一个三倍的差距

落在 `lib/l3/platforms.ts`（纯函数，8 条测试）· `lib/api/counts.ts` 的
`platform` 参数（4 条测试）· `TaskDetailView` 三处渲染。前端 196 passed（原 184）。

**§4.0 四条落地对应到代码：**

| § | 落在哪 | 单平台时 |
|---|---|---|
| 1 分母摊开构成 | `denominatorParts` → KPI note `20 = DeepSeek 10 + 通义千问 10` | 返回空数组，那行不出现 |
| 2 分平台面板 | `platformBars` + `counts?group_by=platform`（一次请求） | 不渲染 |
| 3 矩阵强制单平台 | 矩阵单独一次 `group_by=prompt&platform=X` | 复用 byPrompt，不额外取数 |
| 4 缺口跨平台合计 | 缺口那次**不带** `platform` | 同左 |

> ⚠️ **§4.0 第 3 条和第 4 条对同一份数据的要求是冲突的。**
> 改之前 `rows`（矩阵）与 `gaps` 都从同一份 `byPrompt` 派生 —— 矩阵要单平台、
> 缺口要跨平台，一份数据满足不了两边。**必须拆成两次取数。**
> 这条写在这里是因为下一个人重构取数时很容易又把它们合回去，
> 而合回去之后**界面看起来完全正常**，只是缺口清单会随平台按钮悄悄变。

**run 297（首次双平台，20/20 全成功、单一环境指纹）验证了这件事值得做：**

| | 提及率 | 被提及后排第一 |
|---|---|---|
| DeepSeek | 70.0% (7/10) | **1/7 = 14.3%** |
| 通义千问 | 70.0% (7/10) | **3/7 = 42.9%** |
| 混算（P2-09 之前只能看到这个） | 70.0% | **4/14 = 28.6%** |

**两个平台的提及率一模一样，而首位提及率差三倍 —— 混算的 28.6% 把它完全抹平。**
这正是 §4.0 那句「混出来的 50% 没有操作价值」的字面兑现，
P2-09 上线后第一次双平台 run 就把它演出来了。

⚠️ **但别拿它下结论**：首位率的分母是 7，`1/7` 与 `3/7` 差的是两条样本。
这是「值得再采一轮」的信号，不是「千问上安踏更强」的定论。

顺带一个成本事实：**DeepSeek 平均 16.7 秒、千问 33.3 秒**，跑大批量时差一倍。

#### ⚠️ 2026-08-17：DeepSeek 账号被禁言 —— 「一年没遇到」这个前提死了

run 298 里 DeepSeek **0/21 全败**，报错全是
`Page.wait_for_selector: waiting for locator("textarea")` 卡满 30 秒。
挖了六轮 DOM 才定位：应用挂载了、侧栏与历史全渲染、**输入区从未生成**
（整页 HTML 里连 `textarea` 这个字符串都没有），匿名访问却完全正常。

**真相在手机端**：

```
由於違反使用者規範，您的帳號已被禁言至 2026/8/20 12:09
```

时间吻合：run 297 跑完 ~11:52、run 298 开始 14:51，禁言从 12:09 起 ——
**正好落在两次 run 之间**。因果链清楚：**是我们的采集把一个真实账号打到了禁言。**

> **诊断教训：桌面网页版什么都不说，只是静默不渲染输入区；原因只在移动端露出。**
> 以后遇到「功能静默失效」，**先用手机看一眼**比在 DOM 里挖六轮便宜得多。

**这是第三个平台用同一种手法**（都不报错、不弹验证码，让某个功能静默失效）：

| 平台 | 软阻断形态 |
|---|---|
| 豆包 | 收下消息，**永远「正在生成」**，从不给答案 |
| 千问 | 文字打得进去，**回车不提交**，无任何提示 |
| DeepSeek | 侧栏与历史全渲染，**输入区不渲染** |

相对好的一面：这是**禁言而非封号**，有明确到期时间，8/20 会自己恢复 ——
比豆包那种没有可见期限、还在持续收紧的状态要好。

**当前处境：豆包暂缓 · DeepSeek 禁言到 8/20 · 只剩千问在产数据。**
这三天适合做 P2-34（它不依赖任何平台）。

#### run 298 的另外三个发现（samples=3 那一轮）

**1. 千问首次出现失败**：4/21 超时，签名是
`正文 0 字符（停止回答按钮=True）`—— 和豆包同款静默阻塞，**但它自己恢复了**
（失败集中在 07:13–07:22，07:24 起又连续成功）。
所以千问不是免疫，是**耐受度高得多**；samples=3 把它推到了边缘。

**2. P2-07 报了假警报。** `overall=expired`，其中千问标 `expired`
而它 07:32 还在成功抓取 —— 判据「最早过期的 cookie」抓到的是
`passport_csrf_token` 这类短命的会话辅助 cookie。
**这条 2026-08-16 就预判过并写进了本文，现在应验了。**

**3. P2-36 出现幻影环境。** `environment_id` 有 2 个（41 + 1），
按规矩「长度 > 1 就是混了两个出口」该告警 —— 但 env 2 的 `exit_ip` 是 `None`，
只是那一次探测失败。**出口没混，告警是假的。**

> 后两条性质相同：**告警说了假话。** 这比功能缺失严重 ——
> 一个会撒谎的告警系统，用几次之后就没人看了，而那正是 P2-07 存在的全部意义。

**对 P2-38 的结论**：samples=3 这个数没问题，问题是 42 条跑了 **40.4 分钟**、
两个平台交替串行。要到公开建议的 7–8 次就是 98 条 / 90+ 分钟。
**P2-34 是 P2-38 的前置，不是并列项** —— 这一轮把它坐实了。
run 298 本身不能当数据用（DeepSeek 零样本 + 提问集刚改）。

### 第三梯队 —— 规模与信任

| ID | 任务 |
|---|---|
| **P2-10** | 审计日志 |
| **P2-11** | `counts` 改 SQL 聚合 |
| **P2-12** | 三个列表接口补分页 |
| **P2-13** | N+1 与缺失的 `selectinload` |
| **P2-14** | 定时执行 run |
| **P2-15** | 通知（run 完成 / 大面积失败） |

### 未排期 —— 有价值但不挡路

`P2-16` ~ `P2-27`，见下表。

---

## 3. 完整任务表

按第一轮讨论的八个分类归档，ID 与上面的梯队表一一对应。

### 零、采集出口与部署拓扑

**一条先查到的事实决定了整个方案：`api` 与 `crawler` 共享同一个 volume。**

```yaml
api:      volumes: [crawl_data:/data]
crawler:  volumes: [crawl_data:/data]
```

截图靠这个工作：crawler 写 `/data/screenshots`，api 从**同一个 volume** 读出来
经 `/v1/media/screenshots/{basename}` 提供。所以「只把 crawler 搬走」不成立 ——
搬走之后截图写在大陆，新加坡的 api 读不到，**证据页截图会 404**。

另外 `postgres` 只绑 `127.0.0.1:5433`（这个姿势是对的），所以隧道是**必需**的。

| ID | 任务 | 说明 |
|---|---|---|
| P2-32 | **采集出口对照实验** | ✅ **2026-08-14 完成**，结论见下 |
| P2-33 | **隧道迁移（临时）** | ✅ `scripts/crawl-node/deploy.sh`（image / start / verify / logs）+ README（四条约束、冷备切换、已知问题）。**自检里最重要的一条是「容器出口 ≠ 代理出口」** —— 那是唯一会静默毁掉整件事的错误。⚠️ **crawler 的代码在镜像里**，更新代码只能重传镜像（原先那个 `sync` 子命令推到一个没被挂载的目录，2026-08-15 已删） |
| P2-35 | **`POST /runs` 接受 `note`** | ✅ body 可选（老调用方不带 body 照样能发起）。前端在「立即运行」的确认那一步加了输入框 —— **发起的那一刻才最清楚这次和以往有什么不同**。⚠️ **run 215 的 note 仍是 null**，那是迁移当次的基线断点，补不了了（没有事后修改接口，也不该为此加一个） |
| P2-34 | **HTTP worker（正式）** | crawler 不碰数据库：`GET /v1/worker/lease` 领 job · `POST /v1/worker/jobs/{id}/result` 回传（含截图 multipart）· `POST .../fail` 回传失败原因。**采集节点只需要一个 API key，不需要数据库凭证**；只走 HTTPS 出站；截图问题自然解决；天然多节点 |
| P2-36 | **记录采集环境**（原名「记录采集节点」） | **优先级上调（2026-08-15）。** 原本只想记「哪台机器采的」，但这一轮证明要记的更多：run 215 / 292 / 293 之间**出口 IP、时区、登录态签发地、WAF 类型全都不一样**，而数据里唯一记得住的是 `run.note` 这个自由文本。**没有它，任何跨 run 对比都不可信** —— 包括 P2-32 那个「出口影响回答」的结论本身。建议随 run 存一份环境指纹：采集节点 · 出口 IP · 时区 · 登录态签发地与年龄 |

#### P2-32 的结论（2026-08-14）

| 问题 | 答案 |
|---|---|
| 出口 IP 影不影响回答 | **影响** —— 大陆出口的竞品构成明显更贴合大陆市场（目视确认） |
| 家宽能不能稳定跑 Playwright | **能** —— 约 25 秒/条 |
| 隧道通不通、够不够快 | **通** —— tailnet 直连 116ms、21MB/s |

对照数据：run 27 / 54（新加坡机房）vs **run 215**（长沙移动家宽）。
提及率同为 60.0%，但首位提及率 28.6% → 16.7%（n 小，不足以下结论，
真正的差异在竞品构成）。

**计划外的发现：冷启动会被限流。** run 215 头一分钟的两条 `Page.goto`
120 秒超时，后 8 条全顺，手动重试后 10/10。所以 **P2-16 自动退避重试从
「不挡路」升级成「挡路」** —— 不做的话每次 run 都会莫名丢掉头几条。

> **⚠️ 2026-08-15 更正：上面那句「冷启动会被限流」大概率写错了根因。**
> 真正的原因很可能是**登录态是从新加坡导出的**，详见 §3「零」下面新增的
> 「登录态带地理位置」。换成大陆重建的会话之后，run 293 **20/20 零失败**
> （此前是 2/10、1/10）。
>
> P2-16 仍然值得留着当安全网（登录态迟早再老化），但它**不再是接豆包的前置阻塞**。

#### 登录态是带地理位置的（2026-08-15，代价最大的一条）

`storage_state` **不只是「登录了没有」，它记录着「你是从哪儿登的」。**
迁移时我们把 VPS（新加坡）导出的那份直接拷到大陆节点用了三天。两份会话一对比：

| | 新加坡导出 | 长沙导出 |
|---|---|---|
| WAF cookie | `aws-waf-token`（**AWS WAF**，且**已过期 11 天**） | `HWWAFSESID` / `HWWAFSESTIME`（**华为云 WAF**） |
| `searchEnabled` | `true` | `false` |

**DeepSeek 的境内与境外流量走的是两套不同的基础设施。** 我们一直拿着一张
AWS 的、而且过期了的门票去敲华为云的门 —— 每次 run 头几条卡在 WAF 挑战上。

同时发现第二处「切了 IP 没切环境」：**浏览器上报的时区一直是 UTC**。
`DeepSeekWebProvider` 只设了 `locale="zh-CN"`，从没设 `timezone_id`，
Playwright 就取了容器的系统时区。于是服务端看到的是
**「IP 在长沙、语言中文、时区在伦敦」**。

两处一起修之后（`CRAWL_TIMEZONE_ID` + 在大陆重建登录态）：

| run | 样本 | 失败 | 国际/大陆 提及比 |
|---|---|---|---|
| 215 | 10 | 2 | 0.96 |
| 292 | 9 | 1 | 1.64 |
| **293** | **20** | **0** | **1.19** |

**三条结论：**

1. **「迁移出口」不等于「改 IP」。** 至少还有：浏览器指纹（时区/语言）、
   登录态的签发地、以及服务端按签发地下发的功能开关与模型通道。
   **只验 IP 就宣布迁移完成是不够的** —— `deploy.sh verify` 当时四项全过。
2. **重建登录态必须从目标出口做。** 办法见
   `scripts/export_storage_state.py`：开一条到采集节点的 SOCKS 隧道
   （`ssh -o ExitOnForwardFailure=yes -D 18080 -N -f`），
   `--proxy socks5://127.0.0.1:18080`。脚本会在登录**之前**打印出口 IP。
3. **品牌构成 1.64 → 1.19 但没回到 0.96，且样本数不同，不足以下结论。**
   P2-32 那个「出口影响回答」的结论本身也建立在 n=10 的目视对比上 ——
   要坐实它需要 P2-36（把采集环境记进数据）之后再做一次对照。

#### 这次踩出来、必须固化的操作经验

| 事 | 结论 |
|---|---|
| **镜像别从 mcr 拉** | 家宽直连 136KB/s、走代理 386KB/s → 2GB 要 **90 分钟**。**从 VPS `docker save` 经 tailnet 传只要 66 秒**（21MB/s） |
| **代理：build 走，run 绝不走** | 家里云上 mihomo 在 7890。走代理出口变成香港 `42.200.231.233`，直连才是 `36.157.231.164`（长沙移动）。**crawler 容器不设任何 proxy 环境变量** |
| **postgres 不改绑定** | 仍只绑 `127.0.0.1:5433`，靠 `tailscale serve --tcp 5433` 暴露给 tailnet。公网已验证连不上。**别为了省事改成 `0.0.0.0`** |
| **家里云不是空闲机器** | 上面跑着 tradepod（带 API key）· mihomo · jellyfin · qbittorrent。**任何 `pkill`/`pgrep` 都要按精确 PID**，模式匹配会误伤 |
| **部署会把冷备静默拉起来**（2026-08-14 发现并修） | 旧判断是「容器存在就 `compose up -d`」，而 `docker ps -a` **连已停止的容器一起匹配** —— 于是每推一次代码就把 VPS 那台冷备 crawler 启用一次，而没有任何地方会报错。P2-35 那次部署实际让它跑了 4 小时（碰巧没人发起 run 才没污染）。已修：`deploy/deploy.sh` 对停止态走 `compose create --build`（重建但不启动）。**教训是通用的：靠一条手动 `docker stop` 维持的纪律，会被自动化流程无声撤销** |
| **部署 hook 曾在执行中改写自己**（2026-08-14 修） | 旧 hook 在 `cp` 覆盖 `$GIT_DIR/hooks/post-receive` 之后继续执行，而 bash 按字节偏移续读 —— 沙箱复现证实：**`docker compose up` 与全部健康探测整段被跳过，`git push` 照样报成功**。已拆成「存根 + `deploy/deploy.sh` 正文」，正在执行的文件永不被改写；另加 `flock` 防并发 push 在上一次跑到一半时覆盖 `deploy.sh`。见 `BACKEND.md` §10.2 |

#### 领取的原子性：已核实，比文档写的好

`services/crawl_jobs.claim_pending_jobs` 的 docstring 写着
「simple, single-worker safe enough for demo」——**这句低估了实现**：

```python
select(CrawlJob).where(status == "pending").with_for_update(skip_locked=True)
```

`FOR UPDATE SKIP LOCKED` **本来就是多 worker 安全的**，配合 600 秒的僵死回收
（`running` 超时 → `failed`，刻意不回 `pending`，理由见那个函数的 docstring），
机制是完整的。**P2-34 的 lease 直接沿用这个模式即可，不必另造。**

#### 但备份链路只能冷备，不能热备

技术上两个 crawler 可以并行且不抢同一条 job。**但那样一次 run 的样本会同时
来自新加坡和大陆两个出口** —— 而迁移的全部理由就是出口影响回答。

更隐蔽的是：**数据里没有任何字段记录某条样本是哪台机器采的**（P2-36），
混了就分不开、事后也查不出来。这比「口径变化混进表现变化」还深一层。

**所以 VPS 那台 crawler 是冷备**：平时停着（`docker stop geo-crawler`），
家里云挂了才启（`docker start geo-crawler`）。**启之前必须意识到：
从那一刻起采集出口换回了新加坡**，那段数据与前后不可比。

**隧道期间截图怎么办：暂时关掉**（`SCREENSHOT_DIR` 置空）。证据页那个「查看截图」
按钮本来就是条件渲染的，没有 `screenshot_path` 就不显示 —— **降级是干净的，
不会出现 404**。并在 `run.note` 里写明「这次运行未采集截图」。

**P2-34 有一个必须做对的细节：lease 的原子性。** 两个 worker 同时领任务不能领到
同一个 job —— 要用 `UPDATE ... WHERE status='pending' ... RETURNING` 单条 SQL 占位，
不是「先 SELECT 再 UPDATE」。且 lease 要有到期时间：worker 领了任务然后家里断电，
那批 job 不能永远卡在 `running`（现有的 `CRAWL_STUCK_JOB_SEC` 就是干这个的，直接复用）。

### 一、采集能力

| ID | 任务 | 现状 → 目标 |
|---|---|---|
| P2-06a/b/c | **接豆包 → 千问 → Kimi** | `providers/` 下只有 `deepseek_web.py` 与 `fake.py`。**一个一个来**，每个写 Provider + `registry.py` 加一行。最终 4 个平台。**都要在迁移之后做**（§1.1） |
| P2-07 | **登录态健康度** | **下一步。** 原以为过期表现是「一批 job 全红」—— 2026-08-15 证明**更常见的形态是悄悄降级**：还能抓，只是头几条超时、且服务端按签发地给了另一套环境（§3「零」）。所以要看的不只是「上次成功抓取距今多久」，还要有**「这份登录态是从哪儿签发的」**（WAF cookie 类型可直接判定）与**「里面最早过期的 cookie 还有多久」**。多一个平台就多一份会悄悄过期的凭证 |
| P2-08 | **失败原因分类** | ✅ **后端已落**：`services/failure_kinds.py` 七类 + `crawl_jobs.failure_kind` 列 + `CrawlJobOut` 出口。取值表见 `API.md` §8.0.1。**前端展示没做** —— 留给 P2-09，它会重排样本表版式，现在做要返工 |
| P2-16 | **自动退避重试** | ✅ `services/retry_policy.py`（曲线与决策，纯函数）+ `crawl_jobs.fail_job()`（落库）+ `claim_pending_jobs` 的时间闸门。只重试 `timeout` / `rate_limited`；耗尽后终态 failed。**`CRAWL_AUTO_RETRY_ENABLED` 默认 false**，见下 |

#### P2-16 / P2-08 分两次上线，第二段还没做

**代码已经全在 main 上，但 `CRAWL_AUTO_RETRY_ENABLED` 还是 `false`。**
这是有意的：

1. **先只带分类上线**，跑一次真实 run，`grep kind=unknown` 看分类认得准不准 ——
   尤其是限流那条到底归成了 `timeout` 还是落进 `unknown`。
2. 确认之后再把开关打开，那才是 P2-16 真正生效。

理由和「迁移排在接平台之前」是同一条：**调试时不能有两个变量同时在动**。
分类和重试一起开，出问题时分不清是分类归错了还是退避写坏了。

> **run 215 那两条失败的原文已经不可考** —— 它们被手动 retry 成功了，
> 而 `claim_pending_jobs` 会清空 `error_message`；采集节点容器也重启过，
> 日志只剩一行。所以分类的消息模式是照**现行代码里的真实字符串**写的，
> Playwright 那条走的是异常类型（已在采集节点容器里核过形状），不靠字符串。
> 第一步观察那一轮的价值就在这里：拿到真实样本再决定要不要调模式。

**开关打开的动作**：在采集节点上给 crawler 容器加 `CRAWL_AUTO_RETRY_ENABLED=true`
重启即可（`scripts/crawl-node/deploy.sh`）。回滚就是把它改回 `false`——
**不需要 git revert**（§6 规矩 1）。

### 二、指标缺口（都是「前端算不出」类）

| ID | 缺什么 | 现状 | 挡住了什么 |
|---|---|---|---|
| P2-17 | **顺位分布** | `counts` 只有 `m_first`（一阶段补的），没有分布 | 矩阵格里的 `#N`。**拿 `m_first` 反推名次是编数据**，所以现在只显示 `m/n` |
| P2-18 | `RawResponseOut.run_id` | 没有 | 证据页无法校验 URL 里 `runId` 与 `rid` 自洽（现在是回避掉的：`runId` 只用来生成返回链接、不参与任何数字）。**5 行的活** |
| P2-19 | `sentiment` | 恒 `NULL`，等接 LLM | 情感维度整条线 |
| P2-20 | `citations` | 全库 0 行，根因是采集时从未开联网搜索 | 引用分析整页。**见 §4 的决策项** |
| — | 推荐率 | `is_recommended` 恒 `False` | 已拍板**不做这个指标**（`API.md` §9） |

### 三、性能与规模

| ID | 任务 | 现状 |
|---|---|---|
| P2-11 | `counts` 改 SQL 聚合 | 现在把所有 `RawResponse`（含 `full_text` 全文）拉进 Python 再累加。样本上千后咬人，**多平台会让它提前到来** |
| P2-12 | 列表接口补分页 | `/v1/brands`、`/v1/users`、`/v1/prompts` **三个都没有 `limit`/`offset`**，全量返回。现在数据少看不出来 |
| P2-13 | N+1 与 `selectinload` | `list_tasks` / `list_runs` 每行一次子查询（200 条约 400 次）；`latest_run` 缺 `selectinload`（`get_run` 有） |

### 四、运维与安全

| ID | 任务 | 说明 |
|---|---|---|
| P2-10 | **审计日志** | 改竞品集、删提问词、改别名**都会改写结论**（前两个一阶段亲手确认过），而现在没有任何记录说明谁在什么时候改的。这和 Run 快照是同一个哲学：**结论必须可回溯**。对「商业级」这是硬需求 |
| P2-21 | 用户自助改密码 | 现在只有超管能改别人的，用户改不了自己的 |
| P2-22 | `workspace` 做成真实体 | 现在是个**裸整数**，没有表、没有管理界面。建品牌和建用户时全靠人肉记住数字。A2/A4 里都写了警告文案，但根子上是模型缺一层。**与 §4 的多租户决策绑定** |
| P2-23 | 收紧 `_not_found` 文案 | detail 能区分「不存在」与「不属于你」，是个存在性旁路 |

### 五、生命周期缺口

| ID | 任务 | 说明 |
|---|---|---|
| P2-14 | **定时执行 run** | 全靠手点。「周度监测」这个产品概念现在只存在于任务名里 |
| P2-15 | 通知 | run 完成 / 大面积失败，现在只能自己去看 |
| P2-35 | **run 发起时带 note** | 现在没有任何接口能写 `run.note`，所以「这次运行换了采集出口 / 未采集截图」这类口径说明**记不上**。发起时带比事后补更对 |
| P2-24 | **取消 run** | 发起了撤不回，跑歪了只能等它跑完 |
| P2-25 | 删除 run / task | **见 §4 的决策项** —— 删 run 会级联删证据 |

### 六、前端交互补完

| ID | 任务 | 说明 |
|---|---|---|
| P2-02 | **任务编辑入口** | ✅ 任务头「编辑」展开面板。**没有品牌这一项** —— 后端 `TaskUpdate` 里就没有 `brand_id`，任务过不了户；放个改不动的下拉框只会让人以为改得动 |
| P2-03 | **运行中自动刷新** | ✅ 6 秒一次，**只在有 run 未跑完时开**，且**页面不可见时暂停** —— 否则一个开着不管的标签页会整夜按秒打接口，而没有任何人在看 |
| P2-09 | **多平台改造** | 依赖 P2-05 的口径决定。至少涉及：KPI 行、命中矩阵、任务头的平台 chip |
| P2-26 | 竞品顺序调整 | 顺序 = 矩阵列顺序，现在只能全取消重选 |
| P2-27 | Topbar 标题 | 静态表只到二级路由，`/tasks/[id]` 显示「GEO 监测台」 |
| P2-28 | 移动端 | 一次都没验过 |

### 七、测试与找 bug

| ID | 任务 | 说明 |
|---|---|---|
| P2-01 | **端到端冒烟** | ✅ 分两层。L1 在真实数据上验不变量（只读，可指向生产库）；L2 用真浏览器走客户只读路径 —— **用客户账号是刻意的**：客户纯只读，`require_write` 在服务端挡着，脚本写错也写不进生产 |
| P2-29 | 组件测试 | **优先级刻意低**：判断逻辑已经挤进 `lib/l3` 且各有测试，组件里剩的主要是 JSX 与取数编排 |
| P2-30 | `verify_l2` 进测试 | 脚本存在但是手动跑的 |

### 八、产出

| ID | 任务 | 说明 |
|---|---|---|
| P2-04 | **缺口清单导出 CSV** | ✅ **前端生成，不加后端端点** —— 缺口判级（`findGaps`）只在前端存在，后端没有 gap 这个概念，加端点就要把判级重写一遍。四个坑各有用例：公式注入（安全）· UTF-8 BOM（否则 Excel 中文乱码）· RFC 4180 转义 · **`m/n` 拆成两列**（Excel 会把 `3/5` 转成「3月5日」） |
| P2-31 | **跨 run 趋势图** | `apps/web/README.md` §5 写着「等跑出三次以上再加」。现在有 run 27 与 run 54 **两次**了 —— 第三次之后就该做。这是 Run 快照设计的收成：比的是**表现变化**，不是口径变化。<br>**必须能表达基线断点**（§1.1）：采集出口迁移之后的 run 与之前不可比，一条跨越迁移点的折线画成连续的就是骗人 —— 至少在那个位置画一条竖线标「基线变更」 |

---

## 4. 产品决策

### 4.0 已拍板：多平台口径（P2-05）

**结论：平台是筛选器；「全部平台」这一档保留混算的总数，但把分母的构成摊开。**

推导的地基是一条区分 —— **取决于那块东西是用来诊断还是用来行动的**：

| | 用途 | 跨平台怎么办 |
|---|---|---|
| 提及率 / 首位提及率 KPI | 诊断 | **必须能分平台**。混出来的 50% 没有操作价值，运营要知道的是「豆包上我不行」 |
| 命中矩阵 | 诊断 | **强制单平台**。加一维就是三维，而三维矩阵不可读 |
| **覆盖缺口清单** | **行动** | **跨平台合计** |

缺口清单跨平台合计的理由：它的产出是「去哪些提问下**补内容**」，而 GEO 的机制是
内容在互联网上、AI 去读它 —— **补内容这个动作是平台无关的**，你不会为豆包单独
写一篇文章。而且合计之后样本翻倍，判级更稳：分平台时 n=3，**一个样本 = 33 个
百分点**，缺口判级会随机跳。

四条落地：

1. **KPI 的分母行在多平台时展开成构成**（`35 = DeepSeek 30 + 豆包 5`）。
   这是 §3.1「凡显示比率必须同时显示 m/n」的自然延伸 —— 单平台时 `21/35` 够了，
   多平台时不够，因为构成才决定这个数的含义。
2. **新增「分平台表现」面板**，一根 emphasis 条 per 平台。数据源
   `counts?group_by=platform`，**一次请求**。它直接回答多平台带来的唯一新信息。
3. **命中矩阵强制单平台**，上方带平台按钮，默认第一个。
4. **缺口清单保持跨平台合计**，副标题里说明。

**为什么留混算的总数**：客户会问「我在 AI 里的可见度是多少」，给不出总数的报告
在商业上是残缺的。**为什么必须摊开构成**：那个数会自己动 —— 两个平台表现完全
没变，豆包挂掉一半（`30+30 → 30+15`）就能让总数从 50% 涨到 60%。摊开构成之后，
任何人一眼看出「分母的构成变了，不是表现变了」。

### 4.0.1 已重新拍板：从「必须开联网」改成「记录联网」（P2-37）

> **2026-08-15 原拍板：「客户不认不联网的结果，所以生产抓取必须开联网」。**
> 2026-08-17 的实测与情报调研推翻了它的可执行性，**同日重新拍板**：
>
> > **采用「记录 search 是否激活；无搜索输出是结果，不是废样本」这条标准。**
>
> 原文保留在 git history 里，这里是重新推导后的结论。

#### 一、三家的联网可控性（实测）

| | 有没有开关 | 依据 |
|---|---|---|
| **DeepSeek** | **有**（「智能搜索」，快速模式下） | 外部情报；本仓 §7.0 原本也假设它存在 |
| **豆包** | **没有** | 2026-08-15 实测工具栏全是技能，「快速」是模型选择器 |
| **千问** | **没有** | 2026-08-17 枚举输入区**全部 24 个按钮**：`研究`/`代码`/`翻译` 等都是技能模式（带 `aria-pressed`），无一是联网开关 |

**所以「生产采集必须开联网」这条承诺，只在三分之一的平台上兑现得了。**

#### 二、更要命的一条：我们的提问词恰恰不触发联网

千问实测，**同一账号、同一天**：

```
"2026年杭州有哪些新开的博物馆？"       → 7 篇来源   ✓ 联网
"2026年8月新能源汽车销量排名前五？"     → 10 篇来源  ✓ 联网
"国产运动鞋品牌有哪些值得买的？"        → 0 篇来源   ✗ 未联网
run 296 全部 10 条品牌类提问           → 0 条带来源
```

**时效性问题触发，品牌对比类问题不触发。** 而监测集全是后者 ——
就算三家都能开，对这套提问词的影响也可能远小于预期。

#### 三、行业与学术都不要求「必须联网」，而是要求「记录并分层」

调研（2026-08-17）结论：**没有事实标准，也没有任何一家商业 GEO 产品把
「必须联网」写成有效性规则。**

- **Peec / Otterly / 清蓝**：都不要求联网。Otterly 唯一写进产品原则的是
  「非个性化、不带浏览历史」
- **Semrush**：只对 ChatGPT 要求 search mode，且没说失败重采
- **学术侧明确反对丢弃未联网样本**（选择偏差）：Martinez 综述（2026-07-15）
  的措辞是「**必须记录 search 是否激活；无搜索输出是结果，不是废样本**」
- **公开数据里没有「同一平台开/关网，品牌提及率差多少」** ——
  要这个数只能自己做配对实验

#### 四、因此 P2-37 重新定义：**从「开联网」改成「记录联网」**

| 原计划 | 重新定义 |
|---|---|
| provider 点开关强制联网 | **不强制。** DeepSeek 可选开（唯一有开关的），豆包/千问按平台自身策略 |
| 开了 = 现有基线全部作废、整套重抓 | **基线不作废。** 只是补上「当时未联网」这个标注，按 `search_used` 分桶报告 |
| `_extract_citations` 移到 SSE 分支 | **仍然要做，而且是唯一的路** —— 见下 |

**这个改动把 P2-37 从「必须整套重抓」降级成「加两个字段」，成本差一个数量级。**

#### 五、引用提取：DOM 是死路，SSE 是活路（2026-08-17 实测拿到结构）

千问的引用**一直在流里，只是浏览器不渲染**（与 NDSS 2026 论文
*Characterizing the Implementation of Censorship Policies in Chinese LLM Services*
的观察一致）。实测确认：

```text
端点   POST https://chat2.qianwen.com/api/v2/chat   （SSE）
帧     data:{...,"data":{"messages":[{...}]}}
引用   messages[].mime_type == "bar/progress"
       meta_data.match_num           ← 来源数（= 页面「N 篇来源」）
       meta_data.list[] 每条:
         title / url / raw_url / name（站点）/ publish_time / summary
```

**比原本对 citations 的要求还多**（多了站点名、发布时间、摘要）。

三条排除掉的死路，别再试：

1. **DOM 里没有引用链接。** `[class*="search-content-"]` 那块只有 favicon 图，
   三种点击方式都展不开，外链 0 条。
2. **favicon 反查不出来源。** `s2.zimgs.cn/ims?key=<base64>` 解出来是
   **图标自己在 CDN 上的路径**（`cdn.sm.cn/temp/....PNG`），不是被引用的网页。
3. **class 名全带 CSS Modules 哈希**（`search-content-iMifAk`），
   只能用前缀 `[class*="search-content-"]`，同豆包 `md-box-root` 那个教训。

**DeepSeek 与豆包不必从零写**：开源项目 `daijinma/geo_marketing`（LLM Sentry）
已经用拦 SSE 的方式做完这两家的引用提取。**千问它没做** ——
公开圈没有任何项目从 `qianwen.com` 抽出过引用，上面那份结构是本仓自己测出来的。

#### 六、同日的提问集调整（2026-08-17）：10 条 → 7 条

**砍的是冗余，不是低分。** 原先「跑步」占 4 条明显超配，「泛品牌」与
「价格/性价比」各有一对近义词。停用 `#25 500元左右` · `#27 排行榜前十` ·
`#28 跑步新手`，留下 7 个互不重叠的场景：

```text
#23 跑步-泛  #24 泛品牌  #26 篮球  #29 马拉松
#30 学生性价比  #31 大体重  #32 健身房
```

**四条缺口提问（23/29/31/32）一条没砍** —— 它们才是产品唯一能直接执行的产出。
尤其 `#32 健身房` 历史命中 **0/12**，是最清晰的一个缺口。

⚠️ **这会造成一次基线断点，而且是个假信号：**

| | n | 安踏命中 | 提及率 |
|---|---|---|---|
| 10 条时 | 124 | 76 | **61.3%** |
| 7 条后 | 88 | 49 | **55.7%** |

**提及率掉 5.6pp，而安踏表现一个字没变** —— 因为砍掉的三条里有两条是
100% 命中的。这和 §4.0「分母的构成变了，不是表现变了」是同一类陷阱，
只不过这次动的是提问集。**此后与 run 27…297 的数字不可直接比**，
新 run 的 `note` 必须写明。

> **停用不是删除。** 删会级联删掉 job→response→mention，历史 run 的提及率
> 当场变。已验证：停用后 run 27（21/35）· 296（7/10）· 297（14/20）数字不变。

**顺带**：提问词从 10 减到 7，正好给 P2-38 腾出预算 —— 同样的采集量可以把
`samples` 从 1 提到 2，而这个方向比多几条提问词重要得多。

#### 六、顺带浮出来一个比联网更重要的问题：样本量

调研里反复出现的一条：**n=10 远低于任何公开建议。**

| 来源 | 建议 |
|---|---|
| Schulte et al.（2026-04） | 每 prompt **先重复 7–8 次**当起点 |
| SparkToro / Fishkin（2026-01） | 跑 **60–100 次**才见稳定频率 |
| Gumshoe（2026-02，Bernoulli 推导） | 单 prompt 单模型 **±10pp 需 ≈97 次**，±5pp 需 ≈385 次 |

而我们现在是 `samples=1`、每 run 每平台 10 条。run 297 那个
「首位提及率 14.3% vs 42.9%」的差异，分母只有 7 —— **差的是两条样本**。

**这条不属于 P2-37，但它比 P2-37 更影响结论的可信度。** 单列成 P2-38 待排。

### 4.1 仍待拍板

这几项定不下来，对应的编码任务就不该开始。

| 决策 | 关键取舍 |
|---|---|
| **情感接哪个 LLM** | 成本、延迟、口径稳定性。接完 `sentiment` 才有值 |
| **多租户做到什么程度** | `workspace` 继续当裸整数，还是做成真实体（P2-22）。影响客户规模上限 |
| **删除 run / task 给不给接口** | 删 run 会**级联删掉证据**，历史结论跟着变。可能的答案是「只给归档，不给删」 |

---

## 5. 怎么用这份文档

1. **开工前先看 §4.1。** 如果这一项挂在某个未拍板的决策下面，先去把决策做掉。
2. **一项一个分支、一步一个 commit**，commit message 写清根因 / 改动 / 验收。
3. **完成后回来更新本文**：把该项标 ✅ 并写清落在哪个文件；如果实现过程中
   推翻了这里的判断，**改这里的判断**，别让它留着误导下一个人。
4. 现状类的结论（口径、结构、纪律）不写进本文 ——
   它们属于 `BACKEND.md` / `API.md` / `apps/web/README.md`。本文只管「接下来做什么」。

---

## 6. 回滚规范（迁移这段尤其要守）

`git push vps main` **就是上线** —— post-receive 直接 build 并重启，中间没有闸门。
迁移涉及部署拓扑变更，所以这段的回滚要提前想好。

### 三条规矩，按有效性排序

**1. 尽量「只加不改」。** 新增 compose profile、新增 worker 端点，
**旧路径保持能用**。这样回滚 = 停掉新容器，**根本不需要动代码**。
这是最有效的一条：**能不用 git 回滚的，就别依赖 git 回滚。**

**2. 数据库改动必须向后兼容。** P2-34 若要给 `crawl_jobs` 加 lease 字段，
**加可空列**，不改现有列 —— 这样隧道模式的旧 crawler 仍能工作，
两种模式可以并存过渡，而不是「切过去就回不来」。

**3. 迁移前打 tag。**

```bash
git tag p2-before-migration      # 已打，指向 4a0fd70（P2-04 收工）
```

出事时 `git revert` 有个明确的对照点，而不是去 log 里翻。

### 回滚动作

**不要 `push --force` 到部署仓** —— post-receive 会拿到一个 history 不一致的仓库。
用 revert：

```bash
git revert <bad-sha> && git push vps main    # 重新 build，几分钟
```

代价是重新构建。**所以真正的安全网是规矩 1** ——
让危险的东西活在容器编排层，而不是代码层。
