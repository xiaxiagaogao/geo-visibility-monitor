# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**主要用户：品牌方的运营 / 市场负责人。** 场景是坐在办公室电脑前，回答一个具体问题：
「当消费者去问 AI『2026 年跑步鞋买哪个牌子好』时，我家品牌出现了吗？出现在第几位？
AI 是照着谁的网页说的？」他要拿到的产出是**可以直接交给内容/推流团队的一份清单** ——
哪几条提问上我们是缺席的。

**第二类用户：被服务的品牌客户。** 只看自己那一个任务，首页直接落到最新一次运行，
不需要先过一层列表。角色在服务端收敛可见范围，前端按角色隐藏按钮只是体验、不是安全边界。

**第三类读者：技术评审者。** 这个项目同时用于求职展示，会有人花几分钟扫这个站来判断
做它的人的水平。**不为这类读者改产品行为** —— 让真实产品的质量自己说话。

## Product Purpose

反复向中文 AI 助手（当前是通义千问；豆包、DeepSeek 的 Provider 存在但当前任务里没启用）
问同一批提问，把每个回答切成可核对的事实：提到了哪些品牌、本品在第几位出现、
AI 引用了哪些网页。再由这些事实算出品牌在 AI 回答里的**可见度**。

成功的定义：**每一个百分比都能顺着标注回溯到某一条原文的第 N 个字符**。
一个说不清出处的数字，在这个产品里等于没有。

## Positioning

**唯一同行没有的能力：从 `qianwen.com` 的 SSE 流里抽出引用来源。**
公开圈没有第二家做到（见 `docs/CRAWL-INTEL.md`）。千问的引用不在页面 DOM 里 ——
页面上那块只有站点图标、一条外链都没有；引用只存在于流里。

第二个位置差异是口径的诚实：竞品普遍把「没测到」和「测到是 0」显示成同一个东西。
这个产品**在数据模型层面**就把它们分开，并且拒绝用兜底逻辑把空缺填满。

## Operating Context

- 采集不在服务器上跑，在**大陆家宽的一台节点机**上（VPS 在境外，直连会触发平台风控）。
  所以采集是异步的：节点 lease 任务、回传结果与截图。
- 一次「运行（run）」会**冻结三份快照**：提问集、竞品集、平台集。没有快照的话，
  八月给某品牌加一个竞品，七月那次运行的缺口清单会当场重算、历史结论被改写。
- 运行需要几分钟到几十分钟。页面在有 run 未完成时每 6 秒自动刷新（标签页不可见时暂停）。
- 部署是单域名同源（`geo.example.com`，Caddy 按路径分流）。全站需要登录。
- 前端在本机可跑（dev server 端口 3100，`/v1` 代理到线上）；后端只在 VPS 上跑。

## Capabilities and Constraints

**已有能力**：登录/角色 · 任务列表与新建 · 任务详情（KPI / 逐提问分布 / 覆盖缺口 /
命中矩阵 / 样本列表）· 单条回答证据页（按 `first_offset` 高亮 + 引用来源）·
品牌与提问词管理 · 用户管理 · 缺口清单 CSV 导出。

**后端已就绪、界面上还完全没有的两项**：
`GET /v1/citations/domains`（引用域名榜）与 `GET /v1/counts?group_by=search_used`（按联网分桶）。

**当前给不了的（必须走降级态，不是空态）**：
- `sentiment` 恒 NULL —— 显示「暂无情感数据」
- `is_recommended` 恒 False —— **整个「推荐率」指标不做**，不是降级
- counts 没有顺位分布，只有 `m_first` 一个计数 —— **矩阵格只显示 `m/n`，不显示 `#N`**。
  拿 `m_first` 反推名次是编数据。

**术语**（界面上必须守住的用词）：
- **有效样本**：`answer_status = ok` 的响应数。这是**唯一**的分母口径。
- **未标注 ≠ 未提及**：前者是还没跑 L1 标注，后者是 AI 真的没提。
- **联网三态**：`true` / `false` / `null`。**`null` 是「我们不知道」，不是「没联网」**，
  单独一档，不许并进 `false`。
- **首位提及率**：`position_rank == 1` 的样本数 ÷ 被提及的样本数。
  它是**位置事实，不是「AI 首推」** —— 我们不知道 AI 推没推荐它，只知道它先被提到。

**技术约束**：Next 16（standalone Node 运行时）+ React 19。**没有 Tailwind、没有组件库、
没有图表库、没有状态管理库**，纯手写 CSS Modules 与手写 SVG/CSS 图元。
口径派生全部在 `apps/web/src/lib/l3/` 的纯函数里（不碰 fetch 和 React），取数只走
`lib/api/`（不算比率），组件只吃 props、不许直接 fetch。这三层边界是 204 条测试的前提。

## Brand Commitments

名称 **GEO 监测台**，副标「AI 回答可见度监测」。界面语言为简体中文。
支持浅色 / 深色两套主题，可切换并记忆。

**语气**：不替数据说好听的话。空状态与降级态要说清「缺的是什么、为什么缺」，
不能写成「敬请期待」那种让人以为在排队上线的措辞。

## Evidence on Hand

真实生产数据，不需要造假数据（2026-08-23 实测）：

- 品牌 34「安踏」+ 7 个竞品（李宁 / 耐克 / 阿迪达斯 / 特步 / 361度 / 鸿星尔克 / 亚瑟士）
- 任务 27「安踏监测集」，**12 次运行**（run 289–300，2026-08-16 ~ 08-23），多次为 `partial`
- 全时段：191 条响应 / **180 条有效样本**（11 条 too_short）；本品 `m_mentioned = 112`，
  `m_head = 71` · `m_middle = 30` · `m_tail = 7` · `m_first = 25` · `m_citation_only = 4`
- 最近一次 run 300：21 个采样回来 18 条，提及率 12/18，首位 1/12，覆盖缺口 2 条
- **引用：144 条 / 40 个域名。** 榜首 `www.163.com` 12 次来自 12 条样本；
  而 `www.toutiao.com` 9 次只来自 **4** 条样本 —— API 文档里警告的「聚合站偏向」
  在真实数据里看得见，这正是 `n_samples` 必须与 `n_citations` 并列显示的理由。
- **联网分桶实测只有两桶**：`true` n=19、`unknown` n=161。**`false` 桶不存在。**
  界面不能假设三桶都在。89% 的样本我们不知道联网与否 —— 这是这份数据的诚实结论。
- 逐提问是**两极的**，不是平的：国产/性价比/篮球类提问本品 3/3（100%），
  马拉松/健身训练类 0/3（0%）。总提及率是这两极平均出来的。
- 另有品牌 1「土巴兔」：本品 0 提及但多个竞品有命中 —— 零状态的真实用例。

**不许编造的**：价格、客户名单、跑分、承诺的上线时间、产品没有的能力。

## Product Principles

1. **分母是主语，不是脚注。** 凡显示比率，必须同时显示 `m / n`。
   孤零零的百分比在这个产品里没有可信度 —— 这条在类型层面守着。
2. **「不知道」是一等状态。** `null` 分母不可算 → 显示 `—`；`0` 是真实结论 → 显示 `0.0%`。
   联网 `null` 单独成档。把「未测」和「测到是 0」显示成同一个东西，是这个产品的头号错误。
3. **降级态 ≠ 空态。** 「没采到数据」和「这个维度算不出来」必须用不同措辞，
   否则用户会以为采集坏了。
4. **不静默兜底。** 高亮位置只来自 L1 标注的 `first_offset`，对不上就画红色告警、
   写明标注说什么原文实际是什么，**绝不用 `indexOf` 去正文里自己找**。
   静默画错比空着糟得多 —— 用户会拿一段错的原文去跟客户解释结论。
5. **不可比的两段数据不许连成一条线。** 采集条件变了就是口径变了；
   趋势图必须在断点处标明，而不是画得平滑好看。

## Accessibility & Inclusion

- 图表颜色不许单独承载信息，必须配文字或图形形态；
  色阶与「本品 vs 竞品」的对比必须跑过对比度与色觉模拟验证（历史上踩过：
  某组竞品填充色与主色在绿色盲下 ΔE 仅 5.3，实际不可分）。
- 键盘焦点必须可见；`prefers-reduced-motion` 必须尊重。
- 正文与数据字号在浅色与深色两套主题下都要满足对比度要求。
