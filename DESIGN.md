# Design

<!-- impeccable:design-source built -->

**世界：暗色情报终端。**
由用户提供的风格情报钉死（原件收在 `docs/STYLE-BRIEF.md`），**不是** impeccable
骰子分配的结果 —— brief 优先于 roll。方向契约写在 `apps/web/src/app/layout.tsx` 顶部，
以 HTML 注释形式活到生产构建之后。产品事实见 `PRODUCT.md`。

> 一句立场（`STYLE-BRIEF.md` §2，逐字采用）：
> **暗色情报终端。一种强调色。回答流是唯一装饰。登录和监测台共用同一个签名物件。**

路线 A（Linear 语法）做壳，路线 B（密度 / 指挥台）做核心，
路线 C（衬线）只留给将来的报告封面。

> **v1「熏烟纸地震图」（impeccable seed `e13eab31`）已整体作废。**
> 它在 2026-08-23 上过生产，一天后被这份 brief 替换。
> 仓库里不该再有它的痕迹；读到 pencil / sheet / ruling 这类名字就是漏网的。

这份文件**从已经构建出来的东西记录**，不是先立规矩再去辩护。
凡写「实测」的地方都跑过命令或量过 DOM。

---

## 1. 签名物件：Answer Canvas

`apps/web/src/components/canvas/AnswerCanvas.tsx`

一段模型回答：本品首次出现被点亮、竞品次级标记、引用以行内小票浮出、
模型名与时间戳用等宽低对比贴在角落。

**复用三次以上才叫识别度**（`STYLE-BRIEF.md` §6）：
登录页播这一段 → 证据页的「一次提及」还是这一段 → 将来的报告封面还是这一段。

两条从 v1 原样带过来、**不因为换世界而松动**的纪律：

1. **高亮只来自 L1 标注的 `first_offset`**，不做 `indexOf` 兜底。
   组件内部再验一次不变量：`text.slice(offset, offset+term.length) === term`，
   对不上就**不画**。
   ⚠️ 这条自检不是摆设 —— 做登录页时我手数的演示偏移全错，高亮落在了
   「价位」「跑者」上。补了自检之后，写错只会少画，不会画到错的字上。
2. **偏移按码点算**，不按 UTF-16 码元（`lib/l3/text`）。含 emoji 的回答曾因此错位。

**刻度尺（`kit/TickScale`）和它不冲突** —— Answer Canvas 是**内容物件**，
刻度尺是**数据记号**，两者不在同一层。`STYLE-BRIEF.md` §6 反对的是
「同时上地球 + 粒子 + 神经网络 + 扫描线」那种装饰堆叠。
刻度尺仍然承担「分母可见」：轨道宽度固定、按分母切成 n 格、命中的 m 格上墨 ——
提及率 18 格、首位提及率 12 格，肉眼可分。

### 历次运行图：为什么是面积、为什么竞品是须

`STYLE-BRIEF.md` §8.2 指定单色面积图。**面积**而不是折线，是因为这条序列的
量纲是 0–100% 的比率、**零点有意义** —— 面积把「离零点多远」变成可以一眼
估量的体量，而一条 2px 的线在 128px 高的带子里几乎只是装饰。
**单色** = 数据只用一个色相；竞品是中性灰，不是第二个色相。这是 emphasis
（突出一个、其余灰化），不是类目色板 —— 所以 `dataviz` 验证器的
「lightness band / chroma floor」两项 FAIL 是拿类目色板的尺子量它，不适用；
真正适用的三项（CVD ΔE 19.4、正常视力 ΔE 22.6、两色对面均 ≥3:1）都过了。

**竞品从「区间带」改成「每次运行一根须」**，理由不止是给面积让地方：
一条连续的带会把 min/max 在两次运行**之间插值**，而竞品数值只在运行发生的
那一刻存在 —— 中间那段是画出来的，不是测出来的。

图上的值必须**同时**能从表格读到（图例下的「看数值」）。悬停卡不能是
唯一入口：键盘够不着、触屏要长按、读屏软件只看得到一句 aria-label。

---

## 2. 颜色

**暗色是默认，浅色保留**（白天读表 + 将来的导出/打印版）。

| | 暗色（默认） | 浅色 |
|---|---|---|
| 画布 | `#08090B` | `#F6F7F8` |
| 表面 1 / 2 / 3 | `#0E1013` / `#15171B` / `#1C1F24` | `#FFFFFF` / `#F2F3F5` / `#EAECF0` |
| 边框 | 白 8% / 16% | 黑 10% / 20% |
| 文字 1 / 2 / 3 | 白 92% / 62% / 48% | 黑 92% / 72% / 58% |
| **主 CTA** | **白底黑字** 19.92:1 | 黑底白字 |
| 强调色 | `#5CE1E6` 12.68:1 | `#0C6E7A` 5.55:1 |

`#0F172A` 明确没用 —— `STYLE-BRIEF.md` §7.1 把它列为「一眼 Tailwind slate」。

### 五条不能绕过的规矩

1. **文字层级的下沿没有余量。** 白 48% 实测 4.98:1；再淡一档（42%）掉到 4.04，不合格。
   浅色侧黑 58% 是 4.83，55% 只有 4.36。**三级文字不许再调淡。**
2. **强调色只给数据、高亮与状态，不给大按钮。** 主 CTA 用反色纽扣。
   这是它和「又一个暗色 shadcn」拉开距离的关键一步。
3. **浅色的强调色必须另配。** 冷氰原值在白底上只有 1.5:1，压到 `#0C6E7A` 才 5.55。
4. **浅色的三种面必须真的分得开。** 曾经 `--surface-1` 与 `--surface-2` 都是
   `#FFFFFF` —— 于是所有靠 surface-2 表达「次一层」的东西在浅色下全部失效，
   **表格 hover 没有任何视觉反馈**。暗色档没暴露这问题（那边两者差 4–8%）。
5. **顺序色阶只用于不承载文字的填充，格内数值一律 ink-on-surface。**
   三档色阶的中段是个死区（白字 3.92、深字 3.93，两边都够不到 4.5）——
   这条是 v1 实测出来的物理事实，换了世界没变。改色阶要重跑 `dataviz` 验证器。

### 强调色为什么是冷氰（A/B 已做完）

`STYLE-BRIEF.md` §12 把强调色列为一次 A/B。做了，结论**不是好看不好看**：

> 磷光绿 `#3DFF9A` 与语义绿「上涨/成功」`#4ADE80` 的色距只有 **44** —— 基本同色。
> 选它就必须把强调色与语义绿合并成一个，之后「上涨但不是本品」就没有颜色可用了。
> 冷氰 `#5CE1E6` 与它距离 **104**，天然分得开。

用户 2026-08-24 拍板冷氰。**tokens.css 里不留备选色块** —— 用不上的备选迟早被当成还在用的。

---

## 3. 字

三种声音，共 284 KB，全部自托管（不走任何第三方 CDN）：

| 变量 | 字体 | 用在哪 |
|---|---|---|
| `--font-display` | Geist + 思源黑体 SC 静态 600 子集 | 字标 / 页标题 / 面板标题 |
| `--font-text` | Geist + 系统 CJK 栈 | 正文与**用户内容**（提问原文、AI 回答原文） |
| `--font-mono` | Geist Mono | 数字、比率、代号、时间戳、列头 |

- **等宽是功能不是戏服**：这个产品的数字要成列对齐、要能一眼比长短。
- **中文正文不上自托管字**：用户内容无界、不可能子集化。
- 思源用**静态 600 而不是可变全轴**：全轴子集 362 KB、限轴 500–700 仍 322 KB，
  单一 600 只要 193 KB —— 中文体积压在轮廓上不在轴上。粗细层级靠字号与字距做。

> ⚠️ **中文是子集，只含仓库里出现过的汉字。** 改了界面文案要重跑
> `bash scripts/fonts/build-cjk-subset.sh`，否则漏字会单独掉回系统字。

**已知分歧（未压掉，记在这里）**：impeccable 检测器把 **Geist 判为 `overused-font`**
（4 处命中）。而 `STYLE-BRIEF.md` §7.2 恰恰把它列为展示字第一选择。
**brief 优先于检测器**，用户 2026-08-24 确认保留 Geist。

---

## 4. 版式规矩

- **卡片不是默认容器**（§7.3）：能用分割线就不要盒子。面板只有一档底色差 +
  白 8% 描边，**没有阴影**。
- **圆角 2–6px**，指挥台接近直角。
- **一块仪器面板，几个读数，用细线分隔** —— 不是几张一样大的卡片。
- **页面标题归页面自己，顶栏只是工具轨。** 由 `kit/PageHead`（title / lede /
  meta / action）唯一承担 —— 此前六个路由有四种写法，其中两个路由整页没有 h1。
  主操作一律在页头右上，不在面板角上。
- **口径说明默认折叠成一行**（`kit/Notices`）。这些话一句都不能删，但它们曾把
  首屏 307px 占满、把四个读数全推到折叠线以下。折叠不等于藏：摘要行必须带上
  最重的那一条的语气，有警告就是琥珀色。
- **回答原文行宽限死**（中文 35–45 字）。
- **不进分母的表格行整行罩斜纹**，文字标签同时保留 —— 图案与颜色都不许单独承载信息。
- **平台做成硬件开关的样子**，不是 checkbox 清单（§4.4 / §8.1）。

### 明确偏离 brief 的一处

`STYLE-BRIEF.md` §8.2 让监测台首屏回答 Visibility / Position / **Sentiment** / 竞品。
而本产品 `sentiment` **恒 NULL、是明确不做的指标**（见 `PRODUCT.md`）。
照抄等于在首屏摆一个算不出来的数。首屏改用真实存在的四个：
**有效样本 / 提及率 / 首位提及率 / 覆盖缺口**。

---

## 5. 动效

`STYLE-BRIEF.md` §7.4：150–280ms，强调出场到 400ms，`cubic-bezier(0.16,1,0.3,1)`，
**禁止 bounce**。

- **回答流按 token 逐段淡入**（不是逐字打字机 —— 中文长文上又慢又假），
  全程约 2.2s。`prefers-reduced-motion` **直接跳终态**，不是放慢 —— 放慢仍然是运动。
- 「这个数还在涨」：run 未跑完时读数右上角一个呼吸点。
- **刻意没有**：`transition: width`（动 width 触发布局重排，而那条宽度只在
  数据重取时变，动画纯属装饰）。

---

## 6. 浏览器自带的那部分也归这套设计管

选中色、光标、滚动条、焦点圈全部在 `globals.css` 里接管。
`STYLE-BRIEF.md` §11 的失败清单里「暗色只是把背景改成 `#0F172A`」是失败信号 ——
这些细节是暗色做没做到位的分界。

---

## 7. 当前状态与欠账

**全站 13 个路由已统一到这套语言**（登录 + 12 个）。
`legacy-aliases.css` · `components/ui/` · `components/charts/` 已删除 ——
只剩一套 token、一套原语。

**生产状态：v2 已于 2026-08-29 上线**（`e6187a4` 替换 v1 的 `bd62aaf`）。
本地 `main`、工作分支、生产三者同一个 commit。

**验证到了哪一步：**

- `pnpm lint` / `pnpm test`（238）/ `pnpm build`（13 路由）全通过
- impeccable 检测器：**结构类命中 0**，只剩 4 条 overused-font（见 §3）
- **对比度：6 个路由 × 明暗两档 = 12 个组合，0 失败**（含 AA 大字号豁免）
- **CSS Module 类名审计：0 处引用了不存在的类。** 这条是补上的 ——
  上一轮改名漏了 7 个类名，而 CSS Modules 查不到键只返回 `undefined`、不报错，
  那些元素一直以浏览器默认样式渲染（暗色底上的蓝紫下划线）。
- 中文子集 cmap 核对：`src` 里 1047 个汉字缺 0 个
- **端到端 26 条全过**（`apps/web/e2e/`，Playwright）：六个路由冒烟、
  面积图 / 表格孪生 / 折叠提示条的交互、口径不变量、明暗两档对比度。
  ⚠️ 它跑在**生产数据**上（dev rewrites 把 `/v1` 代理到线上），所以夹具里装了
  一道网络层护栏：除登录/登出外任何非 GET 的 `/v1` 请求一律 abort 并让用例失败。
  凭据只从 `E2E_EMAIL` / `E2E_PASSWORD` 来，不给则 skip 而非 fail。
- 线上实测：5 个自托管字体全部 200（上一次部署这里全 404）、方向契约活到了
  生产 HTML、新访客默认暗色（服务端 `<html>` 不带 `data-theme`）

**还没验到的：**

1. **仪表盘的交互逻辑还没系统过一遍。** 这一轮改的是形态、层级与措辞；
   「点哪里会发生什么、加载中/失败/空三态是否一致、筛选与路由怎么配合」
   还没有被当成一件事整体审过。这是下一轮的主题。
