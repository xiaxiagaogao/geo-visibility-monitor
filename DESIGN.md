# Design

<!-- impeccable:design-source built -->

**世界：熏烟纸地震图记录。**
seed `e13eab31` · scope direction · mode operate · code-led（本机无图像生成工具）。
方向契约写在 `apps/web/src/app/layout.tsx` 顶部，以 HTML 注释形式活到生产构建之后
（`grep 'seed e13eab31' apps/web/.next/` 能找到）。产品事实见 `PRODUCT.md`。

这份文件**从已经构建出来的东西记录**，不是先立规矩再去辩护。
凡是写「实测」的地方都跑过命令或量过 DOM。

---

## 1. 为什么是这个世界

不是隐喻好听，是这份数据的原生形态：

- **12 次 run 是一条时间轴**，采集条件变过的地方是纸带上一道**断口**
- **「空白」与「走平的线」在同一张纸上从不混淆** —— 161 条 `search_used=unknown`
  是「没有记录」，0% 提及是「针在走，只是走平了」。这是本产品的头号原则
- **增益即分母**。一条没标增益的地震道是废的，和「孤零零的百分比没有可信度」
  是同一句话
- 红蓝铅笔是地震台的真实做法（P 波红、S 波蓝），不是为了配色好看

上一版是「现代 SaaS 分析后台」：白卡片、KPI 四宫格、teal 主色。
那是类目默认，也是这次要refuse的东西。

---

## 2. 签名元素：刻度尺

`apps/web/src/components/record/TickScale.tsx`

轨道**宽度固定**，按分母切成 n 格，命中的 m 格上墨。一个图元说两件事：

- 上墨比例 = 比率（横着扫读，和进度条一样快）
- 格数与格宽 = **分母**

**实测效果**：同一块面板上，提及率是 18 格、首位提及率是 12 格 —— 肉眼可分。
上一版这两条是等长的无刻度实心条，「比率永远跟着 m/n」只能靠底下一行小字兑现。

三种状态各有画法，**不许合并**：

| 状态 | 画法 | 为什么 |
|---|---|---|
| `n = 0` 不可算 | 一格不画，只留一道虚线 | 画空轨道等于宣称「0%」，而真相是算不出来 |
| `m = 0` 真的是 0 | n 格全空 + 第一格**描红** | 全空且无标记时，它和「还在加载」长得一样 |
| `n > 40` | 实心条 + 每 10 格一道刻线 | 格宽掉到亚像素就是在骗人。这个退化本身也是诚实的 |

**`zeroMark` 开关**：挂零红标默认开，但在矩阵里必须按品牌关掉。
第一版是无条件的，实测结果是每个竞品的 `0/3` 都描红 —— 一屏几十处红色，
红色就不再有信息量。「本品挂零」是结论，「某个竞品这条没出现」是常态。

---

## 3. 颜色

全站只有**两支铅笔**：蓝（本品 / 主操作 / 活动态）、红（告警 / 挂零 / 不变量对不上）。
状态色（琥珀 / 松绿）只做徽章，永远配文字，**绝不单独承载信息**。

两套主题是同一份记录的**正片与负片**，不是「浅色调暗」：

| | 浅色 = 冲印正片 | 深色 = 熏烟原片 |
|---|---|---|
| ground | `#E2E0DA` | `#131009` |
| sheet | `#F0EFEC` | `#1E1915` |
| ink | `#1A1815` (15.4:1) | `#EDE7DA` (14.1:1) |
| 蓝铅笔 | `#1B4870` (8.27:1) | `#8FBBDF` (8.58:1) |
| 红铅笔 | `#9C2E1C` (6.47:1) | `#E8836B` (6.55:1) |

取值与全部对比度写在 `apps/web/src/styles/tokens.css` 的注释里，**改色前先读文件头**。

### 三条不能绕过的规矩

1. **顺序色阶必须跑 `dataviz` 验证器**，浅深各一次：
   ```
   node <dataviz>/scripts/validate_palette.js "#8FAAC3,#4A7BA4,#1B4870" --mode light --surface "#F0EFEC" --ordinal
   node <dataviz>/scripts/validate_palette.js "#A8C6E0,#6193BE,#2C577C" --mode dark  --surface "#1E1915" --ordinal
   ```
   两次四项全 PASS。深色那组**传参顺序是反的** —— 验证器要求按 light→dark 传，
   而深底上的渲染方向是「越亮 = 值越大」（判据是「离底色越远」，不是绝对明暗）。

2. **色阶只用于不承载文字的填充。格内数值一律 ink-on-sheet。**
   这是实测出来的：三档色阶的中段是个死区，白字 3.92:1、墨字 3.93:1，
   **两边都够不到 4.5**。地震图上本来也不把数字写在道里，标注写在道旁边。

3. **本品/竞品那一对只需要过三项，另两项在构造上不可能过。**
   `#1B4870` vs `#8A857C`：CVD ΔE 22.2 ✓ · 常视觉 ΔE 24.7 ✓ · 对比度 ≥3:1 ✓；
   「亮度带」「彩度下限」FAIL 是预期 —— 那是**分类色板专用**检查
   （验证器自己写着 `scope: categorical palettes only`），而竞品填充是刻意做成灰的。
   历史教训：上一版竞品色与主色在绿色盲下 ΔE 仅 5.3，实际不可分。

### 对比度的最紧底色是 `--ground`，不是 `--sheet`

检查轮里抓到两处：`--ink-3` 与 `--amber` 都只对着 `sheet` 验过，
而页头 meta、旁注这些正好坐在更深的 `ground` 上，实测 4.38 / 4.48，**低于 4.5**。
已重定为 `#605B52` / `#84560C`，三种底色都留余量。
**新增语义色时对着 `ground` 验，那是最紧的一档。**

---

## 4. 字

三种声音，两款自托管（不走任何第三方 CDN，共 188 KB）：

| 变量 | 字体 | 用在哪 |
|---|---|---|
| `--font-instrument` | Martian Mono（可变字重，OFL） | 数字、比率、代号、列头、刻度标注 |
| `--font-display` | 得意黑 Smiley Sans（子集 148 KB，OFL） | 字标 / 页标题 / 面板标题，**仅此三处** |
| `--font-text` | 系统栈 | 中文正文与**用户内容**（提问原文、AI 回答原文） |

**为什么等宽承担数字**：这个产品的数字要成列对齐、要能一眼比长短。
等宽是功能，不是「技术感」的戏服。

**为什么中文正文不上自托管字**：用户内容是无界的，不可能子集化；
硬塞一个 1.1 MB 的 CJK woff2 是拿访问者的流量买设计感。

> ⚠️ **得意黑是子集，只含仓库里出现过的汉字。**
> 改了界面文案要重跑 `bash scripts/fonts/build-cjk-subset.sh`，
> 否则漏字会单独掉回系统字，一个词里两种字形。

**字阶有峰值**：readout 44px ↔ 正文 14px（3.1:1）。
上一版最大 30px、正文 14px（2.1:1），全站没有落点，所以扫一眼像任何后台。

---

## 5. 版式规矩

- **一块仪器面板，几个读数，用刻线分隔** —— 不是几张一样大的卡片。
  后者是类目默认版式，也正是上一版的样子。
- **圆角 1–3px**。记录纸是切边的，仪器面板只有很小的倒角。
- **页面标题归页面自己，顶栏只是工具轨**。上一版顶栏有一张静态的
  「路由 → 标题」表，到 `/tasks/[id]` 就退化成显示应用名 —— 页头同时出现两个标题。
- **回答原文行宽限死 `max-width: 42em`**（实测渲染 588px @14px ≈ 42 个汉字）。
  上一版铺满 1140px，一行 80 字，读到行尾找不回行首。
- **不进分母的表格行整行罩斜纹**，文字标签同时保留 ——
  图案与颜色都不许单独承载信息。
- **图例是全站共用的一份记号法**（`Notation`），不是某张图底下的一行小字。

---

## 6. 动效

**一次授权过的动作，不是散落的 hover 效果。**

- 刻度上墨：`background` 200ms，指数缓出
- 「这个数还在涨」：run 未跑完时读数右上角一个 2.4s 的呼吸点。
  上一版每 6 秒静默刷新，数字自己动了没人知道
- `prefers-reduced-motion` 全局尊重

**刻意没有的**：`.denseFill` 上原本有 `transition: width` ——
动 width 会触发布局重排，而那条宽度只在数据重取时变、没有交互会去动它。
impeccable 检测器命中后直接删掉，不是换成 transform。

---

## 7. 浏览器自带的那部分也归这套设计管

选中色、光标、滚动条、焦点圈的默认样式不属于任何设计系统，
全部在 `globals.css` 里接管。窄屏横轨的滚动条单独收细到 4px，
**但不隐藏** —— 它是「右边还有导航项」的唯一提示。

---

## 8. 当前状态与已知欠账

**已换成这套系统的 4 个路由**：任务列表 · 任务详情 · 证据页 · 引用榜（新增）。

**还在旧原语上的 7 个**：品牌 · 品牌详情 · 新建品牌 · 新建任务 · 用户 · 登录 · 首页分流。
它们靠 `apps/web/src/styles/legacy-aliases.css` 撑着 —— 那是**只做名字映射、
不引入新取值**的过渡层，配色会跟着换但版式仍是旧的，看起来和新页面不是一套。
**这是预期的中间态。** 删除条件写在文件头。

**验证到了哪一步**（实测，不是目测）：

- 自写对比度审计跑遍 4 个路由 × 2 套主题 = 8 次，**全部 0 失败**
- impeccable 检测器：**新代码 0 命中**；剩余 2 条都在 `components/ui/ui.module.css`
  （旧原语，第二批一并清）
- 窄屏 489px：4 页横向溢出为 0
- `pnpm lint` / `pnpm test`（238）/ `pnpm build` 全通过

**还没验到的**：

- **375px 没有直接跑到** —— 那台 Chrome 窗口最小只能到 489px。
  CSS 里的 min-width 逐条复查过，但这不等于在真机上验过。
- **没有组件级 / 端到端测试。** 238 条全是纯函数；改版的回归全靠
  lint + 手工截图 + 浏览器里跑的审计接住，那不是网。
